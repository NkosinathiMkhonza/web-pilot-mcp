# Optional: Switch search engine (default: duckduckgo — no key needed)
SEARCH_ENGINE=duckduckgo

# Optional: Brave Search API key (2000 free queries/month)
# Get one at https://brave.com/search/api/
BRAVE_API_KEY=

# Optional: Fetch timeout in milliseconds (default: 10000)
FETCH_TIMEOUT=10000